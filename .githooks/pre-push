#!/bin/sh
# Refuses to push when the committed ABI no longer matches the contract sources.
set -e

if ! command -v forge >/dev/null 2>&1; then
  echo "pre-push: forge not found, skipping the ABI check (CI still enforces it)." >&2
  exit 0
fi

node packages/contracts/scripts/artifacts.ts abi --check
